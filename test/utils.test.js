const { expect } = require('chai');

describe('Util tests', () => {
  describe('utils/mongoose-uri.js', () => {
    it('should create valid string with no username and password', () => {
      expect(1 + 1).to.equal(2);
    });
  });
});
